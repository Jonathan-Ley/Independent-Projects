/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // domains: ["media.istockphoto.com"],
    domains: ["encrypted-tbn0.gstatic.com", "encrypted-tbn0.gstatic.com"],
  },
};

module.exports = nextConfig;
