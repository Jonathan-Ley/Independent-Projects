/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // domains: ["media.istockphoto.com"],
      domains: ["d3ui957tjb5bqd.cloudfront.net"],
  },
};

module.exports = nextConfig;
